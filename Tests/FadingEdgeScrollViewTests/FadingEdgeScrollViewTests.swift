import XCTest
@testable import FadingEdgeScrollView

final class FadingEdgeScrollViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FadingEdgeScrollView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
